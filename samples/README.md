# GhostGraph demo input

`demo-vulnerable-app` is intentionally unsafe and exists only to demonstrate the scanner. Zip its contents and upload it through GhostGraph to show static-analysis findings for a hard-coded secret, unsafe `innerHTML`, and `eval`.
